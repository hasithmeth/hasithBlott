/**
 * Credentials of a user
 * @property {string} firstName - First name of the userV
 * @property {string} lastName - Last name of the userV
 */
interface User {
  firstName: string;
  lastName: string;
}
